{
  "name": "bugreel-extension",
  "version": "1.0.0",
  "description": "Firefox extension for bugreel — DOM/console/network recording",
  "scripts": {
    "setup": "node setup.js",
    "build": "node build.js",
    "build:firefox": "node build.js firefox",
    "build:chrome": "node build.js chrome"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "rrweb": "^2.0.0-alpha.17"
  }
}
